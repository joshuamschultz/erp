<%= form_for(@test_package) do |f| %>
  <% if @test_package.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_package.errors.count, "error") %> prohibited this test_package from being saved:</h2>

      <ul>
      <% @test_package.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= simple_form_for @test_package do |f| %>
  <%= f.input :name, label: 'Name', placeholder: '1111', required: true %>
  <%= f.button :submit %>
<% end %>
