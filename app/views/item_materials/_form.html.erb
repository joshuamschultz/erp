<%= simple_form_for(@item_material) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :item %>
    <%= f.association :material %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
