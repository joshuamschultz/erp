<%= render :partial => 'attachments/relational_attachments_process_type', :locals => {list_path: process_types_path, new_path: new_process_type_path, name: "process"} %>


<script type="text/javascript"> 

$(document).ready(function (){

var myjson;
$.getJSON("http://erp.chessgroupinc.com/process_types", function(json){
    myjson = json;

   	var $myTable = $('table');
	hideColumn($myTable, 2); 
});


});
function hideColumn($table, columnNumber){
		
	$table.find('td:nth-child('+columnNumber+'), th:nth-child('+columnNumber+')').hide();

	// setTimeout(function() {
	// 	$table.find('td:nth-child('+columnNumber+'), th:nth-child('+columnNumber+')').hide();
	// }, 5000);
}
</script> 